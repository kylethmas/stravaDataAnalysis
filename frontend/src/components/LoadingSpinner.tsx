import React from 'react'

const LoadingSpinner: React.FC = () => (
  <div style={{ padding: 32, textAlign: 'center' }}>
    <div className="subtle-text">Loading your year...</div>
  </div>
)

export default LoadingSpinner
